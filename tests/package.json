{
  "name": "qa-editor-tests",
  "version": "0.0.1",
  "author": "Jernej",
  "scripts": {
    "setup": "npm install && npx playwright install",
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.3"
  }
}
